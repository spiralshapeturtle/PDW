.vs/
.idea/
.claude/
.DS_Store
.vscode/tasks.json
CLAUDE.md
.claude/settings.local.json
pdw_vs2017/
x64/
Releases/
Release/
tools/
out/
release/
build/
bin/
build-fix/
# CMake / out-of-source build directories (build, build-fix, build-cmake-check, build-x86, ...)
build-*/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
obj/
*.log
*.sln
*.suo
*.user
*.cache
*.pdb
*.obj
*.exe
*.dll
*.lib
*.app
*.aps
SMTP_AUDIT_REPORT.md

# PDW Live web companion (separate project, not part of the build)
pdw_web/

# RC editor backup files
*.backup-ascii
*.rc.bak

# Working-copy backup files (CLAUDE.md change-workflow: <file>.bak before each edit)
*.bak

CLAUDE.md


# scratch commit message file
.commitmsg.tmp

# Release zip
PDW-*.zip

# Release staging: holds the sanitized pdw.ini and assets used to build release
# zips. Never tracked, so a live/personal pdw.ini dropped here can never be
# committed (see CLAUDE.md: never share a pdw.ini with personal data).
release-template/
